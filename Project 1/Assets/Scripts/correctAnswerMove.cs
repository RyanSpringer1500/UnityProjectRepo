using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class correctAnswerMove : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // display math problems 
        // display player imput text
        
    }

    // Update is called once per frame
    void Update()
    {
        //while(true)
        // player imputs answer and then checks the solutions by compare the value.
        //if(correct)
        // player moves in certain direction = to solution.
        //else
        // display text incorrect
        // if end loop when end goal triggers: meaning player wins 
        // if end loop when msitakes were made by player: meaning game over
        
    }
}
